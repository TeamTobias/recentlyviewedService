# ===================================================================
# Spring Cloud Config bootstrap configuration for the "prod" profile
# ===================================================================

spring:
  application:
    name: recentlyviewedService
  cloud:
    config:
      uri: http://localhost:8888
      name: recentlyviewedService
